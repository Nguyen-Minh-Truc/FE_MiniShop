import type { CategoryFormValues, CategoryItem } from "./types";

export const defaultCategoryFormValues: CategoryFormValues = {
  name: "",
  description: "",
  active: true,
};

export const initialCategories: CategoryItem[] = [];
